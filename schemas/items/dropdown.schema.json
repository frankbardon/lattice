{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lattice.dev/schemas/items/dropdown/1.0.0",
  "title": "Dropdown Input Item",
  "description": "A leaf input item type (E3-S4) that binds a single document/container variable to a fixed set of selectable options. The reference renderer draws it as an Alpine `<select>`; changing the selection sets the bound variable and re-resolves the document so dependent `${var}`/`$var` consumers update live. The dropdown only declares the binding and its options — the variable itself is declared in the document/container `variables` and supplies the effective default; the runtime value flows through the resolver's override path, not through this config. A dropdown never has children.",
  "type": "object",
  "required": ["variable", "options"],
  "additionalProperties": false,
  "properties": {
    "label": {
      "description": "Optional label rendered next to the control.",
      "type": "string"
    },
    "variable": {
      "description": "Name of the variable this control sets. Must be a variable visible in the dropdown's scope (declared at document scope or on an ancestor container); the value chosen here becomes that variable's runtime override on re-resolve.",
      "type": "string",
      "minLength": 1
    },
    "options": {
      "description": "Selectable options, in display order. Each option is the value assigned to the bound variable when chosen, plus an optional human label. At least one option is required.",
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["value"],
        "additionalProperties": false,
        "properties": {
          "value": {
            "description": "The value assigned to the bound variable when this option is selected.",
            "type": "string"
          },
          "label": {
            "description": "Optional display label for this option; defaults to the value when omitted.",
            "type": "string"
          }
        }
      }
    }
  }
}
